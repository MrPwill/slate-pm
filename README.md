# Slate

Minimal in-memory Kanban board built with Next.js, TypeScript, Tailwind, Zustand, `dnd-kit`, and an OpenRouter-backed task generator.

## Run

```bash
npm install
npm run dev
```

Set `OPENROUTER_API_KEY` in `.env.local` to enable AI task generation.

